# 単語道場_仕様

## プロジェクト

単語道場

## 情報の種類

仕様

## 検索キーワード

単語道場、tango-dojo、仕様、単語カード、日本語、タイ語、英語、音声、画像、Google Apps Script、スプレッドシート、Google Drive、lesson、JSON API

## 保存場所

GitHub / projects / tango-dojo

## 内容

【基本仕様】
単語をカード形式で学習するWebアプリ。
URLのlessonパラメータで教材を指定する。

【データ管理】
単語データはGoogleスプレッドシートの「単語リスト」で管理する。
WebアプリはGoogle Apps Scriptを介して単語データを取得する。

【カードデータ】
GASから取得したデータをカードとして使用する。
カードデータには、教材、課、ひらがな、漢字、タイ語、英語、画像ID、メモなどの情報を含む。

【カード表示】
・表面に意味画像と日本語画像を表示する。
・裏面に意味画像、タイ語、英語、メモを表示する。
・カードをクリックして表面と裏面を切り替える。
・裏面からクリックすると次の単語へ進む。
・最後の単語からは最初の単語へ戻る。

【進捗表示】
現在の単語番号と全単語数を表示する。
表面では「日本語」、裏面では「タイ語」を表示する。

【画像】
画像はGoogle Driveで管理する。
Google Apps Scriptから画像表示用URLを取得する。
取得済みの画像はキャッシュして再利用する。
次のカードの画像を事前取得して表示を高速化する。

【画像ID自動入力】
GASによりGoogle Driveの画像ファイルとスプレッドシートのデータを照合し、画像ファイルIDを自動入力できる。

意味画像：
画像ファイル名の数字と「No」を照合する。

日本語画像：
「課No」と「No」を使用して、01_001.png形式のファイル名と照合する。

【課別シート】
「単語リスト」から指定した課名のデータを抽出し、課名ごとのシートを作成できる。
既存の対象シートがある場合は内容をクリアして作り直す。

【API】
Google Apps ScriptのdoGetをWebアプリからのデータ取得用APIとして使用する。

通常のアクセスでは単語データをJSON形式で返す。
imageパラメータが指定された場合は、指定されたGoogle Driveファイルの画像表示用URLを返す。

【音声】
・音声ON/OFFを切り替えられる。
・表面表示時は日本語を読み上げる。
・裏面表示時はタイ語を読み上げる。

【メモ】
設定でメモ表示をON/OFFできる。
ONの場合、裏面にメモを表示する。

【教材】
lessonパラメータによって教材を切り替える。
単語データはGoogleスプレッドシートからGoogle Apps Script経由で取得する。
